version: '3'
services:
    web:
        image: nginx:alpine
#        ports:
#            - 80:80
#            - 443:443
#        networks: 
#            - nginx-net

networks:
  default:
    external:
      name: nginx-net
